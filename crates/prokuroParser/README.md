## Running locally

Option 1: `cargo run -p prokuro-parser` (runs on port 3001)

Option 2: `docker compose up prokuro-parser` (after infra is set up)

Option 3: `scripts/test-parse.sh corpus/raw/openxenium-bom.csv` (curl test)
